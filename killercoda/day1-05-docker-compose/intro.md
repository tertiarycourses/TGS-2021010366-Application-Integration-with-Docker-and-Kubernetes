# Docker Compose

Docker Compose defines and runs multi-container apps from a single docker-compose.yml file.

**What you will learn:**
- Start and manage a service with Compose
- Wire multiple containers together (Flask + Redis)
- Build a full-stack app (web + PostgreSQL + Redis)
