# Kubernetes Pods & Namespaces

A **Pod** is the smallest deployable unit in Kubernetes — it wraps one or more containers.
A **Namespace** provides isolation so teams can share a cluster without stepping on each other.

**What you will learn:**
- Create and inspect Pods using commands and YAML
- Use Namespaces to isolate resources
