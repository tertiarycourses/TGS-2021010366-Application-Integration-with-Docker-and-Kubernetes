# Environment Variables

Environment variables let you configure containers at runtime without rebuilding the image.

**What you will learn:**
- Set default ENV values in a Dockerfile
- Override ENV values with -e at runtime
- Use --env-file for multiple variables
