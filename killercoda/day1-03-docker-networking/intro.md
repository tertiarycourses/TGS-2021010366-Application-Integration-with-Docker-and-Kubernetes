# Docker Networking & Port Mapping

Docker gives each container its own isolated network. You must explicitly connect containers and expose ports.

**What you will learn:**
- Create custom bridge networks
- Connect containers by name (DNS)
- Map container ports to host ports
