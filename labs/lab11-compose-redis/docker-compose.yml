services:
  web:
    build: .
    ports:
      - "8080:5000"
    environment:
      - REDIS_HOST=redis        # the service name below
    depends_on:
      - redis
  redis:
    image: redis:7-alpine
    volumes:
      - redis-data:/data
volumes:
  redis-data:
